---
cloud_service_group: cloud_db
cloud_service_type: cloud_db
name: Count by Project
query:
  aggregate:
    - group:
        keys:
          - name: name
            key: account
        fields:
          - name: value
            operator: count
options:
  chart_type: DONUT