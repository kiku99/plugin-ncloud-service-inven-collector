---
cloud_service_group: Database
cloud_service_type: CloudDB
name: Count by Count
query:
  aggregate:
    - group:
        fields:
          - name: value
            operator: count
options:
  value_options:
    key: value
    options:
      default: 0